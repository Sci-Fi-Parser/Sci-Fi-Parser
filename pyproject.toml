[project]
name = "sci-fi-parser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "docling>=2.90"
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
