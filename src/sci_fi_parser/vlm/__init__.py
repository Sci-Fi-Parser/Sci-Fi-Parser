"""VLM extractor and configuration.

Kept separate from :mod:`sci_fi_parser.accuracy` so anyone needing just the
model client (the pipeline, a one-off script) can import from here without
pulling in the benchmark machinery.
"""
